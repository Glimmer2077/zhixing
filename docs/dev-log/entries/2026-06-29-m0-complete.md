# 2026-06-29 - M0 Complete

## Goal

Complete M0 setup and leave a durable handoff trail for future sessions.

## Files Added

- Project config: `package.json`, `pnpm-lock.yaml`, TypeScript configs, Vite config,
  ESLint config, Prettier config.
- App shell: `index.html`, `src/main.tsx`, `src/app/*`, `src/design/*`,
  `src/strings.ts`.
- Test setup: `src/test/setup.ts`, `src/app/AppShell.test.tsx`,
  `tests/e2e/app-shell.spec.ts`, `playwright.config.ts`.
- PWA scaffold: `public/manifest.webmanifest`, `public/icons/zhixing.svg`,
  `vite-plugin-pwa` config.
- Development log: `docs/dev-log/README.md`, `docs/dev-log/CURRENT.md`,
  `docs/dev-log/entries/*`.

## Commands Run

- `pnpm add @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities idb-keyval motion nanoid react react-dom zustand zod zundo`
- `pnpm add -D @eslint/js @playwright/test @testing-library/jest-dom @testing-library/react @testing-library/user-event @types/node @types/react @types/react-dom @vitejs/plugin-react @vitest/coverage-v8 eslint eslint-plugin-react-hooks eslint-plugin-react-refresh globals jsdom prettier typescript typescript-eslint vite vite-plugin-pwa vitest`
- `pnpm exec playwright install chromium webkit`
- `pnpm exec prettier --check package.json .prettierrc.json tsconfig.json tsconfig.app.json tsconfig.node.json vite.config.ts eslint.config.js index.html public/manifest.webmanifest src tests docs/dev-log`
- `pnpm typecheck`
- `pnpm lint`
- `pnpm test`
- `pnpm test:coverage`
- `pnpm build`
- `pnpm e2e`

## Verification Results

- Formatting: passed.
- Typecheck: passed.
- Lint: passed with `--max-warnings=0`.
- Unit tests: passed, 1 test.
- Coverage: passed, 100% statements and lines on the current measured files.
- Build: passed; PWA service worker and web manifest generated in `dist/`.
- E2E: passed, 2 tests across Chromium and mobile Safari profile.
- Security scan: no `console.log`, `sk-`, `api_key`, or `apiKey` matches in source/config files.

## Notes

- The directory is not a git repository, so no git diff/status summary exists.
- M0 intentionally does not implement the tree domain or card UI.
- Playwright reported `NO_COLOR`/`FORCE_COLOR` warnings from the web server process, but
  tests passed and no app behavior was affected.

## Next Step

Start M1 with the pure tree/domain layer and tests before adding more UI.
