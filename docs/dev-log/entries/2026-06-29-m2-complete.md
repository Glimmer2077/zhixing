# 2026-06-29 - M2 Complete

## Goal

Render the seed tree as a polished read-only card grid with drill-in navigation.

## Added

- `src/features/marks/Mark.tsx`
- `src/features/cards/Card.tsx`
- `src/features/cards/CardGrid.tsx`
- `src/features/navigation/Header.tsx`
- `src/features/navigation/useNavigation.ts`
- Component tests for marks, cards, grid, header, and app-level navigation.
- E2E coverage for root render, drill-in, and back.

## Behavior Covered

- Root seed cards render in a two-column masonry grid.
- Cards show title, optional subtitle, geometric mark, and creation date.
- Tapping any card enters its child grid.
- Header changes from `知行` to the current node title and exposes a back button.
- Leaf nodes show the empty state.
- Add/settings affordances are present but disabled; editing remains out of scope.

## Verification Results

- `pnpm exec prettier --check package.json .prettierrc.json tsconfig.json tsconfig.app.json tsconfig.node.json vite.config.ts eslint.config.js index.html public/manifest.webmanifest src tests docs/dev-log` - passed.
- `pnpm typecheck` - passed.
- `pnpm lint` - passed.
- `pnpm test` - passed: 58 tests across 14 files.
- `pnpm test:coverage` - passed: 95.03% statements, 83.51% branches, 95.58%
  functions, 95.17% lines.
- `pnpm build` - passed and generated PWA service worker output.
- `pnpm e2e` - passed: 4 tests across Chromium and mobile Safari profile.
- Security scan for `console.log`, `sk-`, `api_key`, and `apiKey` returned no matches.

## Notes

- M2 intentionally avoided add/edit/delete, persistence, settings sheet, import/export UI, and
  drag/drop.
- The seed is created in-memory for M2; persistent storage is still a later milestone.

## Next Step

Commit M2, then begin M3 editing.
