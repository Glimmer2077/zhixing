# 2026-06-29 - M1 Start

## Goal

Implement M1 domain core from `SPEC.md` with tests first.

## Scope

- Tree data types and visual derivation helpers.
- Immutable tree operations and selectors.
- Import/export schema plus tree invariant validation.
- First-run seed builder with injected ID/time dependencies.
- Masonry distribution helper.
- Date formatting utility.

## Out of Scope

- Zustand store and IndexedDB persistence.
- Card UI, navigation, animation, editing, drag/drop.
- PWA polishing beyond the M0 scaffold.

## Testing Intent

Unit tests should cover add/update/remove/move/reorder, cycle guards, selector behavior,
visual determinism, masonry balancing, schema/invariant validation, seed shape, and
date formatting.
