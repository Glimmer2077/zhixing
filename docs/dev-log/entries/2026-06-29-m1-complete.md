# 2026-06-29 - M1 Complete

## Goal

Complete the pure domain layer before UI work.

## Added

- `src/features/tree/types.ts`
- `src/features/tree/visuals.ts`
- `src/features/tree/treeOps.ts`
- `src/features/tree/schema.ts`
- `src/features/tree/seed.ts`
- `src/features/cards/masonry.ts`
- `src/lib/date.ts`
- `src/lib/id.ts`
- `src/lib/clock.ts`
- Focused unit tests for all of the above.

## Behavior Covered

- Immutable add, update, remove subtree, move, reorder, children, path, and descendant
  operations.
- Cycle guard for moving nodes into themselves or descendants.
- Schema and tree invariant validation for imports.
- Stable color, mark, and size derivation from node IDs.
- Starter seed generation with injectable ID/time dependencies.
- Two-column masonry distribution and date formatting.

## Verification Results

- `pnpm exec prettier --check package.json .prettierrc.json tsconfig.json tsconfig.app.json tsconfig.node.json vite.config.ts eslint.config.js index.html public/manifest.webmanifest src tests docs/dev-log` - passed.
- `pnpm typecheck` - passed.
- `pnpm lint` - passed.
- `pnpm test` - passed: 43 tests across 9 files.
- `pnpm test:coverage` - passed: 95.76% statements, 86.71% branches, 100% functions,
  95.59% lines.
- `pnpm build` - passed and generated PWA service worker output.
- `pnpm e2e` - passed: 2 tests across Chromium and mobile Safari profile.
- Security scan for `console.log`, `sk-`, `api_key`, and `apiKey` returned no matches.

## Notes

- M1 intentionally avoided Zustand, IndexedDB, UI cards, editing, drag/drop, and import/export
  UI.
- `validateTreeInvariant()` is stricter than the base zod schema and should be used before
  replacing local state from imported JSON.

## Next Step

Commit M1, then begin M2 read-only UI over the seed.
