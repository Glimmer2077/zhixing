# 2026-06-29 - Git Init

## Goal

Initialize version control before moving from M0 to M1.

## Actions

- Initialized a local git repository in the project directory.
- Planned the first commit as the M0 baseline.
- Used local-only git identity `Codex <codex@local>` because no global git user name or
  email was configured.

## Commit Scope

The first commit includes:

- `SPEC.md`.
- M0 app/tooling/PWA/test scaffold.
- Development log protocol and M0 records.
- `pnpm-lock.yaml`.

## Next Step

Confirm the worktree is clean, then begin M1 domain-core planning.
